import React from 'react';
import renderer from 'react-test-renderer';
import { WidthConsumer, WidthProvider  } from '../';

describe('React-Width-Detector', () => {
  test('it returns ReactWidthDetector JSX', () => {
    
  });
});
